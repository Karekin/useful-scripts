部署文档参考：https://streampark.apache.org/zh-CN/docs/get-started/how-to-use
Before starting the local stack, copy `.env.example` to the ignored `.env` and
replace every `CHANGE_ME` value. Never commit the resulting `.env`.
