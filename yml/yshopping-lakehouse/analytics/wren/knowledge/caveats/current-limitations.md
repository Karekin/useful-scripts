## 当前限制

- StarRocks 未列入 Wren OSS 官方连接器清单；本项目暂以 MySQL 协议作为 POC，只有通过兼容性矩阵后才能进入 Pilot。
- 当前 Wren MCP HTTP 模式按官方运维说明应视为本地开发接口，生产访问必须经过独立认证网关。
- GenBI Live、权限、RLS、调度和定时报表不由本项目假定已经具备；正式发布层必须单独验收。
- 元数据与 AI 治理视图缺少独立新鲜度列，相关候选指标在补齐字段前不得升级为 governed。
