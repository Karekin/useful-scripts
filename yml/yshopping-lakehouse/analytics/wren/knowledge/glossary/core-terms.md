## 闭合

“闭合”表示当前事件、金额、数量、幂等与状态约束在对应 ADS readiness 视图中完成对账。它不是生产上线、来源覆盖或业务真实性的同义词。

## 可用库存

`available_quantity` 是规范库存余额中的可用量，由在手、预占、在途及状态规则形成；不得从 WMS 作业记录临时汇总替代。

## 证据范围

`local_test` 表示本地可重复运行证据；`pilot` 表示受控环境代表性样本；`production` 必须有生产只读来源、完整分母、持续对账和审计证明。
